{
  "scripts": {
    "build": "webpack --mode=development --watch",
    "start": "http-server . -p 8080 --ssl --cert server.crt --key server.key -c-1"
  },
  "private": true,
  "dependencies": {
    "@babel/core": "^7.8.3",
    "copy-webpack-plugin": "^5.1.1",
    "http-server": "^0.12.1"
  },
  "devDependencies": {
    "babel-loader": "^8.0.6",
    "babel-preset-env": "^1.7.0",
    "webpack": "^4.16.2",
    "webpack-cli": "^3.1.0"
  }
}
